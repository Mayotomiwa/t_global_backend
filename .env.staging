# Docker (used by docker compose up)
DATABASE_URL=postgresql://postgres:strongpassword@db:5432/nestdb
NODE_ENV=production

# Postgres container
POSTGRES_USER=postgres
POSTGRES_PASSWORD=strongpassword
POSTGRES_DB=nestdb

# pgAdmin
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
