services:
  - type: web
    name: nestjs-api
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: node dist/main   # optional override
    plan: free                      # or starter, etc.
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: nestjs-db
          property: connectionString

databases:
  - name: nestjs-db
    databaseName: nestdb
    user: postgres
    plan: free